---

# Run apt-get update and apt-get upgrade 
# Depends on: none
#   
- name: patching
   hosts: all
   tasks:
   
# Update packages
- name: Update packages
  become: yes
    update_cache=yes

# Upgrade packages
- name: Upgrade packages
  become: yes
    upgrade=dist
